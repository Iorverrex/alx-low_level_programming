#include <stdio.h>
/**
 * main -numbers of base 16 with lowercase
 *
 * Return: always (0)
 *
 */
int main(void)
{
	int n = 97;
	while (n <= 102)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}

